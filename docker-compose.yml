services:
  test:
    build: .
    working_dir: /workspace
    volumes:
      - .:/workspace
      - test_node_modules:/workspace/node_modules
    command: sh -c "npm install --no-audit --no-fund --prefer-offline && npx vitest run"

volumes:
  test_node_modules: